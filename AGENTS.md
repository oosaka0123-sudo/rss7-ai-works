# RSS7 AI Works エージェント運用ルール

このリポジトリは、複数AIでRSS7 AI Worksを安全に保守し、最終的に低リスクなサイト更新を全自動化するための公開リポジトリです。

## 役割

- ChatGPT: PM/司令塔。タスク分類、Issue作成、優先度・リスク判断。
- Jules: 第一実装担当。軽微・定型・低リスクの更新を原則すべて担当。
- GitHub Copilot: PRの独立レビュー担当。
- GitHub Actions: 機械検査、安全ゲート、状態ファイル更新の唯一の自動化層。
- Claude Code: Julesで解決困難な複雑実装・大規模変更・難しいデバッグのエスカレーション先。

## 基本フロー

1. タスクはGitHub Issueを正本とする。
2. 低リスクは `agent:jules` を第一候補とする。
3. Julesは作業ブランチで変更しPRを作成する。`main`へ直接コミットしない。
4. PRはCopilotレビューとActions検査を通す。
5. 低リスクかつ全検査合格の変更だけを将来の自動マージ対象とする。
6. 高リスク変更は `needs-human-approval` 承認なしでマージしない。

## PROJECT_STATE.json

- `PROJECT_STATE.json` はGitHubのIssue/PR/mainを要約した読み取り用スナップショットであり、真の正本ではない。
- 真の正本はGitHub Issue、PR、mainのcommit。
- 初期導入後、AIエージェントは `PROJECT_STATE.json` と `STATUS.md` を直接編集しない。
- 状態更新はGitHub Actionsのみが行う。

## 安全ルール

1. `.env`、APIキー、パスワード、メールフォーム設定、FTP情報、秘密鍵を登録しない。
2. 元ZIP、バックアップ、ログ、サーバー固有設定、未監査のPHP・CGIを登録しない。
3. 本番サーバーへの全同期・削除同期を行わない。公開は明示されたファイルだけを対象にする。
4. 既存URL、SEO情報、Google確認ファイル、`robots.txt`、`sitemap.xml` を無断変更しない。
5. DB変更、大量削除、`.htaccess`、認証、決済、課金、Secrets、ドメイン、デプロイ方式変更は高リスク扱いとし、人間承認を必須にする。
6. 同一Issueの自動再試行は最大3回。3回失敗後は `status:blocked` とする。
7. Phase 1では1プロジェクトにつき自動実装中Issueは原則1件。並列化はPhase 2以降。

## Jules向け実装原則

- 最小差分で直す。
- 既存デザイン・URL・データ互換性を優先する。
- 変更後に差分確認、自己レビュー、テストを行う。
- 不明な仕様を勝手に拡張しない。
- 失敗時は同じ修正を無限に繰り返さない。
- 高リスク領域に触れる必要が出たら停止し、Issue/PRに理由を残す。

## 完了報告

PRには次を記載する。

- 変更目的
- 変更ファイル
- 実行した確認
- 既知の問題
- ロールバック方法
- 関連Issue
