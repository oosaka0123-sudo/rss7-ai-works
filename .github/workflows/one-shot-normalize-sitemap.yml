name: One-shot normalize sitemap formatting

on:
  push:
    branches:
      - seo/automate-sitemap-lastmod

permissions:
  contents: write

jobs:
  normalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: seo/automate-sitemap-lastmod
          fetch-depth: 0
      - name: Normalize sitemap
        run: python scripts/sync_sitemap_lastmod.py
      - name: Remove one-shot workflow and commit
        run: |
          rm .github/workflows/one-shot-normalize-sitemap.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sitemap.xml .github/workflows/one-shot-normalize-sitemap.yml
          git diff --cached --check
          git commit -m "chore: normalize sitemap XML formatting"
          git push origin HEAD:seo/automate-sitemap-lastmod
