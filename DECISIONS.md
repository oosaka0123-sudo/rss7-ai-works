# RSS7 AI Works Decisions

このファイルは重要な設計判断だけを短く残します。古い議論や長文ログはここへ蓄積しません。

## 2026-09-01

### D-001 Jules-first運用
低リスク・定型・軽微なサイト更新はJulesを第一実装担当とする。Claude Codeは複雑案件へのエスカレーション先とする。

### D-002 GitHubをSSOTにする
タスクはGitHub Issue、作業単位はPull Request、確定コードはmainを正本とする。`PROJECT_STATE.json` は読み取り用スナップショットであり、正本ではない。

### D-003 状態ファイルの書き手をGitHub Actionsに限定
初期導入後、AIエージェントは `PROJECT_STATE.json` と `STATUS.md` を直接編集しない。

### D-004 Phase 1は安全優先
Phase 1では自動実装中Issueを原則1件に制限する。並列実装はPhase 2以降に、変更ファイル範囲の競合検査を追加してから解禁する。

### D-005 例外だけ人間承認を残す
最終目標は低リスク変更の全自動更新。DB、大量削除、`.htaccess`、認証、決済、課金、Secrets、ドメイン、デプロイ方式などの高リスク変更だけ人間承認を必須とする。

### D-006 再試行上限
同一タスクの自動再試行は最大3回。3回失敗後は `status:blocked` として人間または上位エージェントへエスカレーションする。
