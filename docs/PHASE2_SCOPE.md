# 95-point redesign Phase 2

- Keep the existing black background and core CSS language.
- Add WORKS consistently to primary navigation on the main public pages.
- Reorganize SERVICES into two clearly labelled groups without removing any existing service.
- Improve mobile readability by using the brighter existing gray baseline where touched.
- Keep API, admin, data and server configuration unchanged.
- Validate with existing CI plus navigation/service grouping contract checks.
